#include <iostream>
#include <string>
#include <vector>

/*
You are given an array of strings words of length n. You need to group all anagrams together, 
but instead of returning the grouped words, return the indices of the original words (0-based) 
that form each anagram group.
*/


using std::cout;
using std::string;
using std::vector;

class Problem {
    
};

int main(){
    vector<string> vec = {"bat", "tab", "tap", "pat", "cat", "act"};
    Problem p;


    return 0;
}